- name: test filters
  hosts: local
  connection: local
  vars:  # string
    topics: linux, shell, ansible
    tagsMap:  # map/dictionary
      Application: payment
      Environment: dev
    tagsList:  # list   
    - key: Application
      value: payment
    - key: Environment
      value: dev
    #    [
    #     {
    #         "key": "Application",
    #         "value": "payment"
    #     },
    #     {
    #         "key": "Environment",
    #         "value": "dev"
    #     }
    # ]

  tasks:
  - name: print greetings
    ansible.builtin.debug:
      msg: "Hi mam, {{ Greetings | default('good evng') }}" 
  - name: string to lists
    ansible.builtin.debug:
      msg: "{{ topics | split (',') }}"
  - name: dictionary to lists
    ansible.builtin.debug:
      msg: "{{ tagsMap | dict2items }}"
  - name: lists to map
    ansible.builtin.debug:
      msg: "{{ tagsList | items2dict }}"
      
      