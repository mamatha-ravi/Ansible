- name: usrname and password
  hosts: local
  connection: local
  vars_prompt:
  - name: username
    prompt: "what is your name"
    private: false
  - name: password
    prompt: "type your password"
    private: true 
  tasks:  
  - name: print username
    ansible.builtin.debug:
      msg: "hello {{username}}"  
  
